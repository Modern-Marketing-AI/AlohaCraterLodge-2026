Apply the following integration update to suites.html, family-suites.html, basecamp.html, and premium-suites.html. This establishes the pre-flight hooks for our Little Hotelier + HotelChamp stack and implements a custom Exit Intent Recapture modal.

Requirements:

Hook 1 (Global Variables): Add a <script> tag in the <head> establishing LH_BOOKING_ENGINE and HOTELCHAMP_ID.

Hook 2 (Data Tracking): Add data-room-type attributes to all 'Secure Dates' buttons (e.g., data-room-type="premium" or data-room-type="basecamp").

Hook 3 (Dynamic Routing): Update the Magma Modal's '[ PROCEED TO FINALIZE BOOKING ]' button to utilize the LH_BOOKING_ENGINE variable via an onclick function rather than a static href.

Exit Intent Logic: Add a secondary modal (id="exitIntentModal") that triggers when the user's cursor leaves the top of the viewport. It should offer a complimentary 4-hour E-Bike deployment to capture abandoning visitors. When they click to claim, it must close the exit modal and trigger the Magma Modal.

Strict Guardrails: Maintain the Volcanic Dark Mode aesthetic. Ensure absolutely ZERO mentions of the lava tube or cave."

HTML
<script>
    // PRE-FLIGHT HOOK 1: Global Staging for HotelChamp & Little Hotelier
    const LH_BOOKING_ENGINE = "https://placeholder-little-hotelier-link.com";
    const HOTELCHAMP_ID = "PENDING_ID";
</script>

<style>
    /* Add to existing CSS */
    .exit-modal-overlay {
        display: none;
        position: fixed;
        top: 0; left: 0; width: 100%; height: 100%;
        background: rgba(0, 0, 0, 0.95);
        z-index: 2500;
        justify-content: center;
        align-items: center;
        backdrop-filter: blur(8px);
    }
    .exit-modal-content {
        background: linear-gradient(145deg, #1a1a1d, #141417);
        padding: 3.5rem;
        max-width: 650px;
        width: 90%;
        border-top: 5px solid var(--lava-red);
        box-shadow: 0 0 60px rgba(255, 69, 0, 0.15);
        text-align: center;
        position: relative;
    }
    .btn-claim-offer {
        display: inline-block;
        background: var(--emerald-green);
        color: #000;
        padding: 1.2rem 2.5rem;
        text-decoration: none;
        font-weight: 800;
        text-transform: uppercase;
        letter-spacing: 2px;
        margin-top: 2rem;
        border: none;
        cursor: pointer;
        transition: all 0.3s ease;
    }
    .btn-claim-offer:hover {
        transform: translateY(-3px);
        box-shadow: 0 10px 25px rgba(16, 185, 129, 0.4);
    }
</style>

<div id="exitIntentModal" class="exit-modal-overlay">
    <div class="exit-modal-content">
        <span class="modal-close" onclick="closeExitModal()">&times;</span>
        <h2 style="color: var(--lava-red); margin-top: 0; text-transform: uppercase; letter-spacing: 2px;">Deploy With Full Mobility.</h2>
        <p style="color: #d1d1d6; font-size: 1.1rem; line-height: 1.6; margin-bottom: 1.5rem;">
            Before you abandon your Basecamp, secure your tactical advantage. Book your dates direct right now and receive a complimentary <strong>4-hour deployment of our Basecamp E-Bike Fleet</strong>. 
        </p>
        <p style="color: #a1a1aa; font-size: 0.95rem;">
            Explore the Kīlauea summit trails in complete silence. Bypass the crowds. Zero friction.
        </p>
        <button class="btn-claim-offer" onclick="claimOfferAndBook()">[ SECURE DATES & CLAIM FLEET ]</button>
    </div>
</div>

<script>
    // Hook 3: Dynamic Redirection for Magma Modal
    function proceedToBooking() {
        window.location.href = LH_BOOKING_ENGINE;
    }

    // Replace the old magma modal href with this onclick:
    // <button onclick="proceedToBooking()" class="btn-finalize">[ PROCEED TO FINALIZE BOOKING ]</button>

    // Exit Intent Logic
    document.addEventListener('mouseout', function(e) {
        // Trigger if cursor leaves the top edge of the browser and hasn't been shown yet
        if (e.clientY < 20) {
            if (!sessionStorage.getItem('exitIntentTriggered')) {
                document.getElementById('exitIntentModal').style.display = 'flex';
                sessionStorage.setItem('exitIntentTriggered', 'true'); // Prevents spamming the user
            }
        }
    });

    function closeExitModal() {
        document.getElementById('exitIntentModal').style.display = 'none';
    }

    function claimOfferAndBook() {
        closeExitModal();
        openMagmaModal(); // Funnels them back into the Readiness Checklist flow
    }
</script>